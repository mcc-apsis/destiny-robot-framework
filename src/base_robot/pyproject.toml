[build-system]
build-backend = "hatchling.build"
requires = ["hatchling"]

[project]
name = "base-robot"
version = "0.1.0"
description = "Framework for DESTINY enhancement robots"
readme = "README.md"
requires-python = ">=3.13"

dependencies = [
    "destiny-sdk>=0.13.0",
    "httpx>=0.28",
    "pydantic-settings>=2.7",
]

[tool.uv]
package = true